const BASE_URL = "http://localhost:3001";

const search = `${BASE_URL}/search`;
const createIndex = `${BASE_URL}/create-index`;

const API_ENDPOINT = {
  SEARCH: search,
  CREATE_INDEX: createIndex,
};

export default API_ENDPOINT;
